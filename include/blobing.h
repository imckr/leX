#pragma once
#include <string>
#include <iostream>

class Blober {
public:
    std::string computeSHA1(const std::string &content);
  //  void createBlob(const std::string &filePath, const std::string &blobDir);
};
